/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  basePath: '/pj-insurance',
  assetPrefix: '/pj-insurance',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;

